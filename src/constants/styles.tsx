import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  border: {
    borderStyle: 'solid',
    borderWidth: 2,
    borderColor: 'red',
  },
});

export const debugBorderStyle = styles.border;
